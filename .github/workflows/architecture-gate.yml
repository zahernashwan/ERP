name: Architecture Gate

on:
  pull_request:
  push:
    branches: [main]

permissions:
  contents: read

jobs:
  architecture-gate:
    name: 🏗️ Architecture Rules Enforcement
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Enforce ARCHITECTURE_RULES.md compliance
        run: bash scripts/check-architecture.sh
