name: CI Audit Check

on:
  pull_request:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  audit:
    runs-on: windows-latest

    steps:
      - uses: actions/checkout@v4

      - name: Run audit generation script
        shell: pwsh
        run: ./scripts/generate-audits.ps1
