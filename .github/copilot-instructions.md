# Copilot Instructions — NoufexERP

## المرجع الحاكم

عند مراجعة أو كتابة كود لهذا المستودع، يجب الالتزام بالقواعد المعمارية المحددة في:

📄 **[`docs/ARCHITECTURE_RULES.md`](../docs/ARCHITECTURE_RULES.md)**

هذا الملف هو المرجع الحاكم لجميع مراجعات PR.

## ملخص سريع

- **Clean Architecture**: الاعتمادات نحو الداخل فقط (`Presentation → Application → Domain`).
- **Domain Layer**: القواعد التجارية فقط — لا أطر عمل ولا بنية تحتية.
- **Application Layer**: CQRS خفيف (Commands/Queries) — لا قواعد أعمال.
- **Infrastructure Layer**: تنفيذ الواجهات المعرّفة في Application — لا منطق أعمال.
- **Presentation Layer**: عرض فقط — لا وصول للبنية التحتية.
- **Bootstrapper**: Composition Root — حقن صريح بدون Service Locators.

## عند مراجعة PR

1. تحقق من اتجاه الاعتمادات (R-DEP-01 إلى R-DEP-05).
2. تأكد أن القواعد التجارية في Domain فقط (R-DOM-01).
3. تحقق أن Commands تُغيّر الحالة و Queries لا تُغيّرها (R-APP-01).
4. تأكد من وجود اختبارات لكل invariant و use-case جديد (R-TST-01، R-TST-02).

## التوثيق

- `README.md` يُولَّد تلقائياً — لا تعدّله يدوياً.
- التغييرات في التوثيق تتم عبر `docs/` ثم `scripts/generate-readme.sh`.
- راجع [`docs/documentation-map.md`](../docs/documentation-map.md) لفهرس كامل.
