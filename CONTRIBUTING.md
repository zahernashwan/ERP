# المساهمة — Contributing

شكراً لاهتمامك بالمساهمة في NoufexERP! 🎉

## كيفية المساهمة

### 1. Fork & Clone

```bash
git clone https://github.com/<your-username>/ERP.git
cd ERP
dotnet restore
```

### 2. أنشئ فرعاً

```bash
git checkout -b feature/my-feature
```

### 3. نفّذ التغييرات

- اتبع [دليل التطوير](docs/development-guide.md) لمعايير الكود وخطوات الإضافة.
- تأكد من التزامك بـ [القواعد المعمارية](docs/ARCHITECTURE_RULES.md).

### 4. افحص محلياً

```bash
# فحص القواعد المعمارية
bash scripts/check-architecture.sh

# تشغيل الاختبارات
dotnet test

# توليد README (إن عدّلت docs/)
bash scripts/generate-readme.sh
```

### 5. Commit & Push

```bash
git add .
git commit -m "feat: add my feature"
git push origin feature/my-feature
```

### 6. افتح Pull Request

- اشرح التغيير في وصف الـ PR.
- تأكد أن CI يمر بنجاح.

## قواعد مهمة

- ⚠️ أي PR يغيّر كوداً في `src/` **يجب** أن يغيّر `docs/` أيضاً (R-DOC-04).
- ⚠️ `README.md` يُولَّد تلقائياً — **لا تعدّله يدوياً** (R-DOC-02).
- ⚠️ أي مخالفة للقواعد المعمارية = **رفض تلقائي** من CI.

## أنواع التغييرات

| البادئة | الوصف |
| --- | --- |
| `feat:` | ميزة جديدة |
| `fix:` | إصلاح خطأ |
| `docs:` | تحديث توثيق |
| `test:` | إضافة أو تحديث اختبارات |
| `refactor:` | إعادة هيكلة بدون تغيير السلوك |
| `chore:` | مهام صيانة |
